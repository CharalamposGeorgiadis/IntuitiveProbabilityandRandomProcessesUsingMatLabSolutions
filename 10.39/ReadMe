10.39: If Y = exp(X), where X ~ U(0, 1), find the PDF of Y. Next, generate realizations of X on a computer and transform them according to exp(X) to yield the realizations of Y. 
Plot the x's and y's in a similar manner to that shown in Figure 10.22 and discuss your results.
